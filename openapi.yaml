# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Gateway API
    version: 0.0.1
paths:
    /say-hello/{name}:
        get:
            tags:
                - Gateway
            operationId: Gateway_SayHello
            parameters:
                - name: name
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.gateway.SayHelloResponse'
components:
    schemas:
        api.gateway.SayHelloResponse:
            type: object
            properties:
                message:
                    type: string
tags:
    - name: Gateway
